# Custom 404 error page with original URL passed to internal handler
# Handler logs the error and redirects to display page
# Use absolute path from document root (works on both XAMPP and live server)
ErrorDocument 404 /404-handler.php?url=%{REQUEST_URI}
